---
description: Show whether Claude Auto IDE auto-continue is enabled
allowed-tools: Bash, Read
---

# /claude-auto:status

Run:

```bash
claude-auto ide status
```

or:

```bash
npx claude-auto ide status
```

Summarize the output for the user in plain language (enabled/disabled, config path).
